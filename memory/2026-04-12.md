# 2026-04-12 日记

## 今日总结

### 核心工作
今天主要围绕两个方向工作：① 建立了视频/网页内容分析的子 agent 路由规则（bibi + web_fetch）；② 持续迭代尤克里里/口琴乐谱网站，重点解决乐谱图片识别和页面导航问题。

### 决策记录
- **子 agent 路由规则**：视频分析走 bibi 子 agent，网页总结走 web_fetch 子 agent，不再占用主 agent
- **乐谱识别方案**：通过图片尺寸 + OCR 内容特征（音符/和弦标记）识别乐谱，建立了 sheetmusic-identifier skill
- **页面分 tab 设计**：口琴和尤克里里通过顶部 tab 切换，详情页独立
- **日记方案**：daily-diary skill 定时 23:00 + 手动触发，详细记录在 memory/detail/，总结引用
- **写代码子 agent 去掉用户确认**，提高效率

### 问题记录
- **改尤克里里页面时破坏口琴详情页**：根因：代码修改时没有隔离不同模块；教训：修改共享组件前先备份和测试
- **乐谱图片识别多次失败**：根因：只基于 HTML 结构特征判断，不可靠；方案：改用图片内容 OCR 分析
- **详情页返回跳转到口琴**：根因：路由状态管理问题，尤克里里详情页返回时状态丢失
- **网站多次部署后仍有访问问题**：根因：部署后未做完整验证；已要求子 agent 今后自验证

### 规则建立
- 今天新建 `sheetmusic-identifier` skill：乐谱图片识别规则
- 今天新建 `daily-diary` skill：日记生成规范（定时 23:00 + 手动）
- 今天设计了 `daily-summary` skill（尚未完全集成到触发链）
- URL 路由规则已写入 TOOLS.md

### 教训
- 改代码时要隔离模块，不能因为改 A 模块顺手改了 B 模块
- 部署后必须自验证，不能等用户报 bug
- 子 agent 执行任务时，主 agent 应监控进度而非干预

### 未完成
- **详情页返回问题**：尤克里里详情页返回后跳转逻辑仍在排查（20:09）
- **部分尤克里里乐谱图丢失**：图片识别方案已建立但批量更新脚本未完全跑通
- **daily-summary skill 尚未集成到触发链**：skill 内容已有但未设置自动调用
- **Obsidian 同步问题**：部分内容未同步到 Obsidian（20:56）

> 📋 详细记录：memory/detail/2026-04-12.md
